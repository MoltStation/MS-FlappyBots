import {
  DESIGN_HEIGHT,
  DESIGN_WIDTH,
  GAME_SLUG,
  GAME_TITLE,
  TICK_HZ,
} from '../game/constants';

export const gameConfig = {
  slug: GAME_SLUG,
  title: GAME_TITLE,
  designWidth: DESIGN_WIDTH,
  designHeight: DESIGN_HEIGHT,
  tickHz: TICK_HZ,
};
